import 'package:flutter/material.dart';

class CardVacina extends StatelessWidget {
  const CardVacina({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
